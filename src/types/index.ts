import { BlockToolData } from '@editorjs/editorjs';

/**
 * tool Tool's input and output data format
 */
export interface toolData extends BlockToolData {}

/**
 * tool Tool's configuration object that passed through the initial Editor config
 */
export interface toolConfig {}